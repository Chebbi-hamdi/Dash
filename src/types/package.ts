export type Package = {
  name: string;
  price: number;
  invoiceDate: string;
  status: string;
};

export type Admin = {
  name: string;
  Emails: string;
  InscriptionDate: string;
  MobilePhone: string;
  Role: string;
};
